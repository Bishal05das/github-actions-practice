name: first workflow

on:
  push:
    branches:
      - main

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
     - name: First Step
       run: |
        echo "First step"

  second-job:
    runs-on: ubuntu-latest

    steps:
     - name: first Step
       run: |
        echo "first step"
