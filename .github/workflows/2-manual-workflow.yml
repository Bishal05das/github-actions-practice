name: manual workflow

on: 
  workflow_dispatch:

jobs:
  manual-workflow:
    runs-on: ubuntu-latest

    steps:
     - name: First Step
       run: |
        echo "First step"
        echo "hello again"